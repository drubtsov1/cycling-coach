.DS_Store
*.swp
*.swo
.idea/
.vscode/
__pycache__/
*.pyc

# Never commit anything personal that might end up in this dir during dev
.env
.env.*
!.env.example
*.local.json

# Personal session-handoff state (conversational layer — never commit).
# Anchored to repo root so templates/state/ is NOT ignored.
/state/
